function addBinary(a: string, b: string): string {
    function add(indexA: number, indexB: number, c: number) {
        if (c == 0) {
            
        } else {
            
        }

        add(indexA - 1, indexB - 1, c)
    }
};